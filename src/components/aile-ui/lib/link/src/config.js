export const DefaultConfig = {
  // 是否支持超出部分省略号显示
  ellipsis: false,

  // Link的最大宽度
  maxWidth: '100%',

  // 自定义inputInner的最大宽度，不传则使用默认计算规则
  // 无icon属性及icon插槽时，默认100%，加入icon属性减去20px，加入icon插槽再减去20px
  inputMaxWidth: ''
}

export const DefaultLinkAttrs = {
  underline: true
}